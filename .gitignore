# Rust build artifacts
/target

# Local embedding index caches (regenerated by `cargo run -- index`)
/.cache
/.cache-*

# Corpus data — bring your own. May be copyright-restricted: NEVER commit.
/data

# Local environment config (secrets, hosts, model names)
.env

# Editor / OS noise
/.idea
.DS_Store

# Local-only Claude Code settings
/.claude/settings.local.json
